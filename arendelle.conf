<source>
  @type http
  port 9880
  bind 0.0.0.0
  body_size_limit 32m
  keepalive_timeout 10s
</source>

elsa = "#{ENV['ELSA_APP']}"

# Matching for Elsa
<match elsa.app.log.**>
  @type stdout
</match>
